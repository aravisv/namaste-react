import React from "react";

const ProjectTeam = (props) => {
  return (
    <div>
      <h1>Project Team</h1>
      <h2>This project is done by {props.name}</h2>
    </div>
  );
};

export default ProjectTeam;
